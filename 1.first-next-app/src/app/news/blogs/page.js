// "use client"; //Write this line to make server component into client component. By default in next app all component are server component.
import React from "react";

const BlogPage = () => {
  return <div>BlogPage</div>;
};

export default BlogPage;
